# OpenClaw v2.1 Released

We just shipped OpenClaw v2.1 with three major improvements:

1. Smart skill chaining — skills can now call other skills in sequence
2. Memory persistence across sessions — conversations carry forward
3. 40% faster Ollama inference via batched requests

This release also fixes the skill-runner timeout issue that affected long-running tasks.

Available now for all users.
